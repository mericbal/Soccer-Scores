<div class="jumbotron">
  <h1 id="tema"> <strong> Soccer Scores </strong></h1> 
  <% if current_user.sign_in_count == 1 %>
		<h3> Welcome to Soccer Scores , <i id="red"> <%= current_user.username %> </i> </h3>
	<% else %>
		<h3> Welcome back , <i id="green"> <%= current_user.username %> </i> ! </h3>
	<% end %>
</div>
